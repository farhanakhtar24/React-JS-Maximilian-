/* THEORITICAL BAATEIN */
/*
We can pass in values to our components using the props(Properties)
In our component we only make a single object as the parameter of the main function which contains all
the properties which we passed to our component from main app and from that we access the properties and manipulate 
our html
*/

'use strict';
